name: 03 - Workflow Runners

on: push

jobs:
    ubuntu-echo:
        runs-on: ubuntu-latest
        steps:
                - name: Show OS
                run: | #pra escrever um unico script bash - com várias linhas]
                    echo "This job is running on an Ubuntu runner."
                    echo "Runner OS: $RUNNER_OS"
